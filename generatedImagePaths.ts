// // Auto-generated path configuration
// export const imagePaths = {
//   categories: {},
//   subcategories: {},
//   default: "../../assets/images/default.jpg"
// } as const;

// export const getImagePath = (id: string, isCategory: boolean = true): string => {
//   const collection = isCategory ? imagePaths.categories : imagePaths.subcategories;
//   const key = isCategory ? `C${id}` : `SC${id}`;
//   return collection[key] || imagePaths.default;
// };
